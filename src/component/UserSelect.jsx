import React from "react";

const UserSelect = () => {
  return <div style={{
    width: '100%',
    display: 'flex',
    justifycontent: 'center',
    alignitems: 'center'
  }}>Please select a user to start the interaction.</div>;
};

export default UserSelect;
